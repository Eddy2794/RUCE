import { AutoridadComisionModel } from './autoridad-comision-model';

describe('AutoridadCooperadoraModel', () => {
  it('should create an instance', () => {
    expect(new AutoridadComisionModel()).toBeTruthy();
  });
});
