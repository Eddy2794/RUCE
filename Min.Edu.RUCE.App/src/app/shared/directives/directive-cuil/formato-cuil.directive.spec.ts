import { FormatoCuilDirective } from './formato-cuil.directive';

describe('FormatoCuilDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatoCuilDirective();
    expect(directive).toBeTruthy();
  });
});
