import { TipoAsociacionModel } from './tipo-asociacion-model';

describe('TipoAsociacionModel', () => {
  it('should create an instance', () => {
    expect(new TipoAsociacionModel()).toBeTruthy();
  });
});
