import { EstablecimientoModel } from './establecimiento-model';

describe('EstablecimientoModel', () => {
  it('should create an instance', () => {
    expect(new EstablecimientoModel()).toBeTruthy();
  });
});
